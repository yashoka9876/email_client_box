import React from 'react'

const settings = () => {
  return (
    <div style={{
        position:'absolute',
        top:'30px',
        right:'30px'
    }}>
        <button>Logout</button>
    </div>
  )
}

export default settings